---
# This pipeline is run for EVERY branch
kind: pipeline
name: default
steps:
  - name: git-tag
    image: alpine
    environment:
      GITHUB_TOKEN:
        from_secret: GITHUB_TOKEN
    commands:
      - apk --update add git
      - git tag v1.4
      - git push origin v1.4