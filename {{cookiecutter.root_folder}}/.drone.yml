---
# This pipeline is run for EVERY branch
kind: pipeline
name: default
steps:
  - name: test1
    image: alpine
    commands:
      - echo {{cookiecutter.function}}
      - echo {{cookiecutter.owner_email}}
